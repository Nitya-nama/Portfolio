Drop real product screenshots here to replace the built-in mockups automatically:

  ecovision.png
  pharmaguard.png
  smartbite.png
  cinerent.png

Referenced from src/data/projects.ts (`image` field). If a file is missing,
the site falls back to a hand-built mockup matching that project's UI type
(dashboard / clinical report / mobile app / ERP table) — never a generic
placeholder graphic.

Recommended: 1600x1000px (or similar 16:10) PNG or JPG, actual screenshots
of the running app for the strongest effect.
